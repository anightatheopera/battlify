services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/code/app          # Maps backend
      - ./frontend:/code/frontend # <--- THIS LINE IS CRITICAL
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload